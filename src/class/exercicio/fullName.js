// nome cpmpleto
const name = 'joão pedro cavanhas colombo'

//usando metodo split para gerar um array separando o nome 
const nameArray = name.split(' ')

// peguei o primeiro nome usando 0 e peguei o ultimo usando o length
console.log('Olá,',nameArray[0],nameArray[nameArray.length - 1])
